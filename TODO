automatically disable .toString() in the Variables view for arrays
export to file doesn't preserve precision
copy to clipboard function
render complex numbers
update DataUnitConverter to have it assembled from a single visitor and a covarying calls so that calls with floats don't need to be converted
